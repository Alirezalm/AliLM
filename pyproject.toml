[project]
name = "alilm"
version = "0.1.0"
description = "My Personal Transformer Language Model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "regex>=2026.5.9",
    "torch>=2.12.0",
    "transformers>=5.9.0",
]

[dependency-groups]
dev = [
    "line-profiler>=5.0.2",
    "notebook>=7.5.6",
]
